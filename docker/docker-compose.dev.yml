version: '3.9'

services:
  db:
    ports:
      - ${POSTGRES_PORT}:${POSTGRES_PORT}

  api:
    command: npm run start:dev
    volumes:
      - ../server:/home/node/app
    ports:
      - ${API_PORT}:${API_PORT}
    environment:
      - API_PORT
